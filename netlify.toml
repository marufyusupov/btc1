[build]
  command = "npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NPM_FLAGS = "--legacy-peer-deps"
  NODE_VERSION = "20"

# Include merkle-distributions in the build
[functions]
  directory = ".netlify/functions"
  node_bundler = "esbuild"
  included_files = ["merkle-distributions/**",
  "deployment-base-sepolia.json"]



# Removed - Next.js API routes are handled by @netlify/plugin-nextjs
